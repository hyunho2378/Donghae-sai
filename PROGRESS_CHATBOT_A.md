# PROGRESS_CHATBOT_A.md 챗봇 컴포넌트 개선 묶음 (세션 A)

작성 2026-08-24. 데이터 파일 미변경. 챗봇 컴포넌트와 프롬프트만. 스트리밍과 출처 버튼 로직 미변경.

## 실행 환경 한계 (먼저 밝힘)

이 환경에는 Ollama/Gemma(localhost:11434)도, 챗봇 백엔드(localhost:3000)도, 연결된 브라우저 확장도 없다.
따라서 실제 LLM 답변 스크린샷은 남기지 못했다. 대신 검증은 아래로 했다.
- 클라이언트 vite build 통과
- 조사 교정 로직 단위 테스트 10/10 통과(아래)
- 마크다운 표 파서 단위 테스트 통과
현호 님 로컬(Ollama 구동 + `npm run dev`)에서 완료 기준 6개를 육안 확인하면 된다. 시스템 프롬프트를 바꿨으니 서버 재기동 필요.

## 작업 1. 조사 버그 — 원인과 해결

원인 진단. **가설 2(LLM 생성 오류)** 확정.
- SovereignChat.jsx, SovereignHero.jsx, AnswerText.jsx, useSovereignChat.js, rag.mjs, chat.js 를 grep 했으나 `${name}은/는` 같은 조사 하드코딩이 **0건**. 답변 텍스트는 전부 Gemma 스트림 원문이다.
- 즉 "카페 한결는"은 모델이 받침 조사를 틀리게 생성한 것이다.

해결(두 겹).
1. 시스템 프롬프트(rag.mjs RULES)에 조사 규칙 + 예시 추가. 받침 있으면 은/이/을/과/으로, 없으면 는/가/를/와/로. 한결은, 묵호항은, 추암은, 바다는 예시.
2. 클라이언트 안전망 `fixJosa`(useSovereignChat.js). 유니코드 받침 판별 `(code-0xAC00)%28`로 조사를 고른다. **볼드로 감싼 장소명 뒤 조사에만 한정**해 멀쩡한 문장을 깨뜨릴 위험을 줄였다. 스트리밍 미완성 볼드(`**한결`)는 매칭 안 되므로 완성 후에만 교정. ㄹ받침(종성 8) 예외로 으로/로 처리. 한글이 아니면 원래 조사 유지.
   - stripMarkdown 마지막에 적용(SovereignHero/SovereignChat 둘 다 경유).

단위 테스트 10/10 통과(대표): `카페 **한결**는 밤 10시까지` → `카페 **한결**은 밤 10시까지`, `**서울**으로`→`**서울**로`, `**부산**로`→`**부산**으로`, `**Cafe**는` 유지.

## 작업 2. 우측 상단 스크롤 화살표 제거

코드 전역 grep 결과 **맨 위로 가기 스크롤 버튼은 존재하지 않는다**. ScrollToTop.jsx는 라우트 변경 시 `window.scrollTo(0,0)`만 하고 UI를 렌더하지 않는다(null 반환). ChevronUp/맨위 FAB 없음.
유일한 위 방향 화살표는 챗봇 입력창의 **전송 버튼 ArrowUp**(기능 버튼, 이전 지시로 위 방향 유지)뿐이라 제거하면 전송이 깨진다.
결론: 제거할 스크롤 버튼이 코드에 없다. 사용자가 본 것은 브라우저 기본 동작이거나 이미 제거된 상태로 판단. 전송 버튼은 유지.

## 작업 3. 하단 안내 문구 변경

SovereignHero 하단 문구를 정확히 교체.
- 전 동해 로컬 데이터로만 답해요. 동해 밖 정보는 모를 수 있어요
- 후 동해 로컬 데이터만 사용해 답하며, 최신 정보와 다를 수 있어요

## 작업 4. 답변 하단 액션 버튼

AnswerText 하단에 ActionBar 추가(복사 공유 좋아요 싫어요, lucide Copy/Share2/ThumbsUp/ThumbsDown).
- 복사 navigator.clipboard.writeText(볼드 별표 제거한 평문). 눌리면 복사됨 + Check.
- 공유 navigator.share 있으면 호출, 없으면 링크(평문) 복사 폴백.
- 좋아요/싫어요 시각 토글, 둘 중 하나만 활성(vote 상태). 활성 시 채워짐(fill). 좋아요 프라이머리, 싫어요 accent.
- box-shadow 없음. 아웃라인/텍스트 색만. DESIGN.md 준수.
- 스트리밍 중에는 감추고(showSources 조건과 동일) 답변 완료 후 노출.

## 작업 5. 마크다운 표 렌더링

AnswerText에 파이프 표 파서 추가. 문단 블록 중 헤더+구분선(--- ) 형태면 HTML `<table>`로 렌더.
- 스타일 DESIGN.md. border border-border-sub, 그림자 없음, rounded-lg(정의된 radius), 헤더 bg-bg-card. 셀에도 볼드 렌더.
- 출처 칩 배정 로직은 텍스트 문단에만 적용하도록 필터링해 기존 동작 보존.
- 시스템 프롬프트: "샵과 콜론과 마크다운 표와 이모지는 쓰지 않는다" → 표 금지 해제. "여러 항목을 조건별로 비교하는 질문에는 마크다운 표로 정리해도 좋다. 단 단순 추천이나 대화형 답변에는 억지로 쓰지 않는다" 추가.
- 파서 단위 테스트 통과(1/2/3일권 비교 표 head/body 정상 파싱).

## 작업 6. 속도와 유사질문 진단

속도(하드웨어 바운드, 구조 정상).
- 시스템 프롬프트 RULES는 프리픽스 선두 배치(프리픽스 캐시 이점). 조사/표 규칙 소량 추가.
- RAG maxHits 3(상위 3개 제한) 확인.
- keep_alive "30m" 확인(chat.js) — 모델 언로드 방지.
- 스트리밍 첫 토큰 즉시 flush 확인(chat.js 토큰별 write, hook 첫 토큰에 loading 해제).
- 결론 Gemma 로컬이라 근본 속도는 하드웨어 한계. 설정은 이미 최적(top-3, keep_alive, 스트리밍). 구조 변경 안 함.

유사질문(키워드 매칭의 약점 + 경량 개선).
- RAG는 임베딩이 아니라 **키워드 매칭**(`query.includes(kw)`). 동의어에 약하다.
- 개선 `expandQuery` 동의어 확장 추가(rag.mjs). 애들/아이/자녀→가족, 잘데/묵을/자는곳→숙소 숙박, 맛집/밥/식당→맛집 먹거리, 뚜벅이/차없/버스→뚜벅이 대중교통, 밤/야간/별→밤 야간, 체험/놀거리→체험 액티비티. 질문을 대표어로 넓혀 키워드 매칭이 유사 표현을 같은 의도로 잡게 한다. 알고리즘 구조는 그대로, 쿼리 전처리만.
- 라이브 일관성 확인(가족 숙소 / 애들이랑 잘 데 / 아이랑 묵을 곳)은 Ollama 구동 후 로컬에서 확인 필요.

## 수정 파일

- client/src/hooks/useSovereignChat.js (fixJosa, batchim, stripMarkdown 적용)
- client/src/components/AnswerText.jsx (표 렌더 + ActionBar, 출처 로직 보존)
- client/src/components/SovereignHero.jsx (하단 문구)
- server/sovereign/rag.mjs (조사 규칙, 표 허용, expandQuery 동의어)

## 완료 기준 로컬 확인 체크리스트 (현호 님)

- [ ] 받침 장소명 조사 정상(한결은/묵호항은/추암은)
- [ ] 스크롤 버튼 없음(코드상 애초에 없음 확인)
- [ ] 하단 문구 교체 확인
- [ ] 답변 하단 복사/공유/좋아요/싫어요, 복사 동작, 좋아요/싫어요 토글
- [ ] "1일권 2일권 3일권 차이를 표로" 질문에 표 렌더
- [ ] 같은 의도 다른 표현 답변 일관성
