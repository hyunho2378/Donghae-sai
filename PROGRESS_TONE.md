# PROGRESS_TONE.md 챗봇 말투 친절체 개편

작성일 2026년 8월 24일. 1부 시스템 프롬프트 재작성 세션 기록.
2부 브라우저 실측은 별도 세션에서 한다. 이 세션에서는 실제 대화 테스트를 하지 않았다.

## 수정한 파일

단 하나다.

- `server/sovereign/rag.mjs`

정확히 어디를 고쳤나.

- `buildSystemPrompt(hits)` 함수 전체를 재작성했다. 파일 31행부터 끝까지다
- 함수 위에 `const RULES` 상수를 신설했다. 말투 규칙 전문이 여기 들어간다
- `searchKnowledge()`는 한 글자도 건드리지 않았다. 검색 점수와 상위 3개 자르기 로직 그대로다
- 함수 시그니처 `buildSystemPrompt(hits)`와 반환 타입은 그대로다. 호출부인 `server/sovereign/chat.js:14`는 무수정

건드리지 않은 것.

- `server/sovereign/chat.js` 전체. sources 줄과 links 맵을 먼저 보내는 NDJSON 스트리밍 로직 무수정
- `client/src/hooks/useSovereignChat.js`. 토큰 이어붙이기와 stripMarkdown 무수정
- `client/src/components/AnswerText.jsx`. 문단 쪼개기와 출처 문단 배정과 상세페이지 버튼 무수정
- 데이터 파일 전량. stays.json packages.json stories.json donghae-knowledge.json 무수정
- 확인 안 됨 표기는 데이터에 그대로 뒀다. 화면 노출은 프롬프트 규칙으로만 막는다

## 무엇이 문제였나

증상은 챗봇이 데이터를 그대로 뱉는 것이었다. 원인은 두 겹이다.

첫째, 기존 프롬프트가 문어체를 지시하고 있었다.

```
문체.
담백하게 정보만 전달하라. 감탄이나 권유를 덧붙이지 마라.
다만이나 또한 같은 접속사를 반복하지 마라.
자료집에 없으면 확인이 필요합니다라고 한 줄로 끝내라.
```

담백하게 정보만 전달하라는 지시가 사용자 대면 대화를 데이터 덤프로 만들었다.
이건 DESIGN.md 340행에서 342행의 톤 항목과도 어긋난다. DESIGN.md는 원래부터 권유형 종결어미를 쓰라고 적혀 있었다. 보세요 해보세요 하실 수 있어요다.
즉 현호 님이 원하는 친절체가 원래 DESIGN.md 규정이고, 기존 프롬프트가 그걸 위반하고 있었다.

둘째, 자료집 문장 자체가 전부 명사형 이다 체다. 이게 더 큰 원인이다.

자료집 193건은 기획서와 화면 데이터를 파서로 변환한 것이라 문장이 이렇게 생겼다.

```
2030 뚜벅이 코스 묵호 사이 파도 사이. 골목 감성과 바다와 액티비티를 한 번에
경험하는 대표 뚜벅이 코스다. DAY 1은 묵호 도착, 동쪽바다중앙시장, 논골담길,
묵호등대, 도째비골 스카이밸리, 해랑전망대, 어달해변 일몰, 저녁, 숙박이다.
```

모델은 컨텍스트 문체를 따라간다. 그래서 말투만 존댓말로 지시해도 부족하다.
자료집 문장을 그대로 옮기지 말라는 지시를 따로 박아야 한다. 새 프롬프트의 핵심이 이 대목이다.

셋째, 내부 표기가 컨텍스트에 그대로 실린다. 실측 수치다.

- 자료집 193건 중 97건에 확인 안 됨이 들어 있다. 총 143회 등장
- 연계 후보는 122회 등장
- 미정과 협의 가능은 0회. 데이터에 없는 표기다

모델이 확인 안 됨을 보면 확인되지 않음이라고 되뇐다. 이게 00숙소 확인되지 않음 답변의 정체다.

## 새 시스템 프롬프트 전문

`RULES` 상수 내용이다. 1,528자.

```
너는 동해사이 여행 도우미다. 동해로 여행 오는 사람에게 말을 건네는 안내자다.

말투.
친절한 존댓말 대화체로 답한다. 이에요 예요 좋아요 있어요 어때요 해보세요 같은 부드러운 어미를 쓴다.
이다 한다 같은 문어체 종결이나 명사만 늘어놓는 문장은 쓰지 않는다.
장소만 나열하지 말고 왜 그곳이 좋은지 이유를 한 마디씩 붙인다.
질문한 사람의 상황에 맞춘다. 가족이라고 하면 아이와 함께 움직이는 상황을, 뚜벅이라고 하면 차 없이 걷고 버스 타는 상황을 기준으로 답한다.
네가 AI라는 말은 하지 않는다.

자료집 문장 옮겨 쓰기 금지.
아래 자료집은 내부 기획 문서라서 문장이 딱딱하다. 사실만 가져오고 문장은 네 말로 다시 만든다.
2030과 4050 뚜벅이와 자차로 나눈 이동 동선 같은 내부 분류를 그대로 말하지 않는다. 차 없이 걸어서 도는 1박 코스처럼 사람이 쓰는 말로 바꾼다.
DAY 1과 DAY 2 같은 표기는 첫날과 다음 날로 바꿔 말한다.

확인 안 됨 처리.
자료집에 확인 안 됨이라고 적힌 항목은 아직 정보가 없다는 내부 표시다.
확인 안 됨 확인되지 않음 미정 협의 가능 이런 말을 답변에 절대 쓰지 않는다.
정보가 없는 항목은 아예 언급하지 않고 넘어간다. 꼭 짚어야 하면 방문 전에 한 번 확인해보시는 게 좋아요처럼 자연스럽게 안내한다.
자료집이 연계 후보라고 적은 곳은 제휴가 아직 확정되지 않았다는 뜻이다. 연계 후보라는 말을 쓰지 말고 필요할 때만 아직 준비 중인 곳이에요 정도로 짧게 덧붙인다.
자료집에 없는 것은 지어내지 않는다. 그 부분은 아직 모른다고 솔직히 말한다.

강조.
장소 이름과 가격과 시간처럼 눈에 걸려야 하는 값만 별표 두 개로 감싼다.
한 문단에 두 곳까지만 감싸고 문장 전체를 감싸지 않는다.
추천한 장소의 이름은 그 장소를 설명하는 문단 안에 반드시 쓴다.

정보 표기.
콜론을 쓰지 않는다. 항목 이름과 값 사이는 빈칸으로만 띄운다.
운영시간 10:00에서 19:00처럼 적는다. 이곳은 오전 10시부터 오후 7시까지 운영합니다처럼 문장으로 풀지 않는다.
2일권 8,000원처럼 적는다. 2일권은 8천원입니다처럼 쓰지 않는다.
숫자는 자료집에 적힌 값을 그대로 쓴다. 바꾸거나 어림하지 않는다.

배치.
한 문단은 세 문장까지다. 문단이 바뀌면 빈 줄 하나를 넣는다.
여러 곳을 안내할 때는 한 곳씩 문단을 끊는다.
샵과 콜론과 마크다운 표와 이모지는 쓰지 않는다.
전체는 여덟 문장 안쪽으로 유지한다.
어울릴 때만 마지막에 후속 제안을 한 문장 붙인다. 매번 기계적으로 붙이지 않는다.

답변 예시.
나쁜 예는 동해사이 패스로 이 코스를 즐긴다. 1일권 5,000원, 2일권 8,000원, 3일권 10,000원이다.
좋은 예는 일회성 NFC 패스예요. 머무는 날만큼 사면 되고 오래 있을수록 하루 값이 싸져요. 1일권 5,000원, 2일권 8,000원, 3일권 10,000원이에요.
나쁜 예는 가족 숙소 확인되지 않음. 선샤인호텔 확인되지 않음.
좋은 예는 가족이 머물기 좋은 곳으로 선샤인호텔이 있어요. 묵호역에서 걸어갈 수 있어서 짐이 많은 가족 여행에 편해요.
```

자료집이 붙는 꼬리 문장.

```
동해 자료집. 아래 사실만 근거로 삼는다.
(자료집 상위 3건)

위 자료집 문장을 그대로 옮기지 말고 친절한 존댓말 대화체로 다시 말해라.
확인 안 됨이라고 적힌 항목은 답변에서 빼라.
```

검색 결과가 0건일 때.

```
동해 자료집.
지금 질문과 맞는 자료집 항목이 없다. 아직 가진 자료가 없어 답을 드리기 어렵다고
친절하게 말하고 다른 것을 물어보라고 한 문장 덧붙인다. 없는 사실은 지어내지 않는다.
```

## 변경 전후 대조

| 항목 | 전 | 후 |
|---|---|---|
| 문체 지시 | 담백하게 정보만 전달하라 | 친절한 존댓말 대화체. 이에요 예요 좋아요 |
| 종결어미 | 이다 한다 문어체 | 이에요 예요 있어요 해보세요 |
| 맥락 | 규칙 없음 | 왜 좋은지 이유를 한 마디씩 붙인다 |
| 질문 의도 | 규칙 없음 | 가족은 아이 동반, 뚜벅이는 대중교통 기준 |
| 자료집 옮겨 쓰기 | 규칙 없음 | 명시 금지. DAY 1과 내부 분류 표현 변환 지시 |
| 확인 안 됨 | 규칙 없음 | 절대 출력 금지. 생략하거나 자연스러운 안내로 |
| 연계 후보 | 규칙 없음 | 그 말 대신 아직 준비 중인 곳이에요 |
| 후속 제안 | 규칙 없음 | 어울릴 때만 한 문장 |
| 자료집 없을 때 | 확인이 필요합니다 한 줄 | 친절하게 사과하고 다른 질문 유도 |
| 답변 길이 | 여섯 문장 | 여덟 문장 |
| 예시 | 정보 표기 예시만 | 좋은 예와 나쁜 예 쌍 두 개 추가 |
| 블록 순서 | 자료집 먼저, 규칙 나중 | 규칙 먼저, 자료집 나중 |

## 의도적으로 유지한 것

앞선 세션에서 실측으로 잡은 서식은 손대지 않았다.

- 별표 두 개 볼드. AnswerText.jsx의 renderInline이 이걸 strong 태그로 그린다
- 콜론 금지. stripMarkdown의 항목 뒤 콜론 제거 안전망과 짝을 이룬다. 시각 표기 10:00은 보존된다
- 문단은 빈 줄 하나로 나눈다. AnswerText가 개행 두 개 이상으로 문단을 쪼갠다. 이 규칙이 깨지면 출처 칩 배정도 깨진다
- 추천한 장소 이름을 문단 안에 쓰라는 규칙 신설. assignSources가 문단 본문에 장소명이 있는지로 출처 칩을 배정한다. 모델이 이름을 안 쓰면 칩이 전부 마지막 문단으로 몰린다
- 근거라는 단어 없음. 출처는 상세페이지 버튼으로만

## 블록 순서를 바꾼 이유

기존은 자료집이 앞, 규칙이 뒤였다. 그래서 질문마다 프롬프트 앞부분이 달라져 공통 프리픽스가 첫 문장뿐이었다.
새 구조는 규칙 1,528자가 앞에 고정된다. 질문이 무엇이든 이 구간이 동일하므로 Ollama가 프리픽스를 다시 평가하지 않는다.
warmup.sh가 데워야 할 구간도 네 질문의 개별 프리픽스가 아니라 공통 규칙 블록 하나가 된다. 워밍업 효과가 전체 질문으로 퍼진다.
recency를 잃지 않도록 자료집 뒤에 두 줄 요약 지시를 다시 붙였다.

## 실측한 것

Ollama 호출 없이 프롬프트 생성만 검증했다. 실제 답변 품질은 2부다.

- `node --check server/sovereign/chat.js` 통과
- buildSystemPrompt 정상 동작. RULES 1,528자. 검색 0건일 때 전체 프롬프트 1,633자
- 질문별 프롬프트 길이. 가족 숙소 2,158자, 2일권 2,707자, 2030 뚜벅이 1박 2,218자
- 규칙이 앞, 자료집이 뒤인 것 세 질문 전부 확인
- 컨텍스트에 실제로 내부 표기가 실리는 것 확인. 묵호에 숙소 있어 질문에서 확인 안 됨 3회와 연계 후보 3회
- 서버 재기동. rag.mjs는 import 시점에 한 번 읽히므로 재기동 없이는 옛 프롬프트가 남는다. pid 9003 종료하고 9101로 재기동. 포트 3000 정상

프롬프트가 1,868자에서 2,200자대로 늘었다. 처음 보는 프롬프트의 첫 토큰은 그만큼 느려진다.
다만 늘어난 부분이 전부 고정 프리픽스라 한 번 데우면 모든 질문이 그 캐시를 공유한다. 2부에서 첫 토큰 시간을 실측해 확인한다.

## DESIGN.md와의 충돌

- 별표 사용. DESIGN.md 334행은 AI 응답에 별표를 금지한다. 현호 님이 앞선 세션에서 볼드를 명시 요청해 유지 중이다. 이번 지시서도 볼드 강조를 유지하라고 했다. 규정 위반 상태를 알고 유지한다
- 톤. DESIGN.md 342행의 권유형 종결어미와 새 프롬프트가 일치한다. 오히려 기존 프롬프트가 위반이었다. 이번 개편으로 규정에 맞춰졌다
- CONTENT_GUIDE.md의 능동태와 명사형 규칙은 화면 카피용이다. 챗봇 답변에는 적용하지 않기로 이번 지시서에서 확정했다

## 2부에서 확인할 것

브라우저 실측 체크리스트다. 히어로 챗봇과 FAB 챗봇 양쪽에서 본다.

1. 확인 안 됨 확인되지 않음 미정 협의 가능이 답변에 0건인지. 특히 숙소 질문에서
2. 연계 후보라는 표현이 0건인지
3. 종결어미가 이에요 예요 좋아요 계열인지. 이다 한다로 끝나는 문장이 0건인지
4. DAY 1과 2030 4050 뚜벅이 자차 같은 내부 분류 표현이 0건인지
5. 장소 추천에 왜 좋은지 이유가 붙는지
6. 가족과 뚜벅이 질문에서 관점이 달라지는지
7. 볼드가 장소와 가격과 시간에만 붙고 문장 전체를 감싸지 않는지
8. 콜론 0건인지. 10:00 같은 시각 표기는 살아 있는지
9. 확정 수치 문자 대조. 5,000원 8,000원 10,000원, 1,220만 9,032명, 14.2퍼센트
10. 출처 칩이 장소를 언급한 문단에 붙는지. 마지막 문단으로 전부 몰리지 않는지
11. 답변이 여덟 문장을 넘지 않는지
12. 후속 제안이 매번 기계적으로 붙지 않는지
13. 첫 토큰 시간. 콜드와 워밍업 후 각각
14. 자료집에 없는 질문에 지어내지 않고 모른다고 하는지

## 미검증 위험

- 모델이 gemma4:e4b다. 규칙 수가 늘어 소형 모델이 일부를 놓칠 수 있다. 2부에서 가장 자주 깨지는 규칙을 찾아 앞으로 올리거나 문장을 줄인다
- 확인 안 됨 차단이 프롬프트에만 걸려 있다. 모델이 새면 화면에 그대로 나온다. 2부에서 누출이 확인되면 렌더 단계 안전망을 추가한다. useSovereignChat.js의 stripMarkdown에 내부 표기 제거를 붙이는 방식이다. 이번 세션 범위가 아니어서 하지 않았다
- 여덟 문장으로 늘린 것이 답변을 늘어지게 만들 수 있다. 2부에서 길면 여섯으로 되돌린다

## 이번 세션에서 하지 않은 것

- 브라우저 대화 테스트와 스크린샷. 2부
- 사이트 전체 카피 대청소. 범위 밖
- 데이터 파일 수정. 범위 밖
- 렌더 단계 내부 표기 필터. 지시서가 프롬프트 규칙으로만 막으라고 함
